<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <data>
        <record id="activity_mail_inherit_contact_view_buttons" model="ir.ui.view">
            <field name="name">activity.mail.inherit.contact.view.buttons</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_form" />
            <field name="priority" eval="34"/>
            <field name="arch" type="xml">
                <div name="button_box" position="inside">
                    <button class="oe_stat_button" type="object"
                            name="action_view_all_activity_control"
                            attrs="{'invisible': [('company_type', '!=', 'company')]}"
                            icon="fa-list">
                        <field string="Activity" name="activity_count" widget="statinfo"/>
                    </button>
                    <button class="oe_stat_button" type="object"
                            name="action_view_all_mail_message_control"
                            attrs="{'invisible': [('company_type', '!=', 'company')]}" string="Mail/Message"
                            icon="fa-list">
                        <!--<field string="Mail/Message" name="mail_message_count" widget="statinfo"/>-->
                    </button>
                </div>
            </field>
        </record>
    </data>
</odoo>